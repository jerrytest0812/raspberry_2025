[project]
name = "raspberry-2025"
version = "0.1.0"
description = "This is a project of raspberry-pi and pico."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=7.1.0",
    "numpy>=2.2.6",
]
